   
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce
    username: root
    password: Pankaj@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


server:
  port: 8765

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

  info:
    env:
      enabled: true
info:
  app:
    name: Order Service
    version: 1.0

#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
#  endpoint:
#    health:
#      show-details: always
#  info:
#    app:
#      name: Order Service
#      version: 1.0
#      description: Product microservice using Spring Boot

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

